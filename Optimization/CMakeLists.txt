PROJECT(Optimization)

FILE(GLOB HEADERS ${PUBLIC_HEADERS_DIR}/*.h)
FILE(GLOB SOURCE *.cpp)

ADD_LIBRARY(Optimization STATIC ${SOURCE} ${HEADERS})

TARGET_INCLUDE_DIRECTORIES(Optimization PRIVATE /)
TARGET_INCLUDE_DIRECTORIES(Optimization PUBLIC ${PUBLIC_HEADERS_DIR})
